# Legacy Coffee Date archive

This directory is the preserved, non-executable Coffee Date implementation.

The active bot does not import this directory, register its handlers, run its jobs,
write Coffee orders, or display Coffee menus. The archived files are retained only
for historical reference and are intentionally outside the active `bot/` package.

Do not add this directory to `sys.path` or import it from the active application.
